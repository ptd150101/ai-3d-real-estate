import type{Metadata}from'next';import{CompareClient}from'@/components/CompareClient';export const metadata:Metadata={title:'So sánh bất động sản'};export default function Page(){return <section className="section"><div className="container stack"><span className="eyebrow">Decision support</span><h1 style={{fontSize:'3rem'}}>So sánh bất động sản</h1><p className="muted">Đặt tối đa bốn căn cạnh nhau để so sánh giá, diện tích, pháp lý và trải nghiệm 3D.</p><CompareClient/></div></section>}
