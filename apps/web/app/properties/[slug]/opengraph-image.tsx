import { ImageResponse } from "next/og";
import { getProperty, formatPrice } from "@/lib/api";
export const size={width:1200,height:630};export const contentType="image/png";
export default async function Image({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=await getProperty(slug);return new ImageResponse(<div style={{width:'100%',height:'100%',display:'flex',background:'#0f2f26',color:'white',padding:70,flexDirection:'column',justifyContent:'space-between',fontFamily:'sans-serif'}}><div style={{display:'flex',fontSize:30,fontWeight:700}}>NESTORA · AI + 3D REAL ESTATE</div><div style={{display:'flex',flexDirection:'column',gap:22}}><div style={{display:'flex',fontSize:64,fontWeight:800,maxWidth:1000}}>{p.title}</div><div style={{display:'flex',fontSize:32,color:'#b8d7cc'}}>{p.district}, {p.city} · {p.area_m2} m² · {p.bedrooms} PN</div><div style={{display:'flex',fontSize:54,fontWeight:800,color:'#74d3ad'}}>{formatPrice(p.price,p.transaction_type)}</div></div><div style={{display:'flex',fontSize:24}}>Xem tour 3D · Panorama 360° · Hỏi trợ lý AI</div></div>,size)}
