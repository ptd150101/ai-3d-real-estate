"use client";
import { FormEvent, useState } from "react";import{clientApi}from"@/lib/client-api";
export default function Page(){const[status,setStatus]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await clientApi('/admin/panorama/scenes',{method:'POST',body:JSON.stringify({property_id:f.get('property_id'),name:f.get('name'),image_url:f.get('image_url'),thumbnail_url:f.get('thumbnail_url')||null,initial_yaw:0,initial_pitch:0,initial_fov:75,sort_order:0,published:true,metadata_json:{}})});setStatus('Đã tạo scene panorama.')}catch(err){setStatus(err instanceof Error?err.message:'Lỗi')}}return <div className="stack"><h1>Panorama 360°</h1><form className="card card-padded stack" onSubmit={submit}><input required name="property_id" placeholder="Property ID"/><input required name="name" placeholder="Tên phòng / scene"/><input required name="image_url" placeholder="URL ảnh equirectangular"/><input name="thumbnail_url" placeholder="Thumbnail URL"/><button className="btn btn-primary">Tạo scene</button>{status&&<p>{status}</p>}</form><div className="card card-padded"><h2>Workflow</h2><p className="muted">Tạo scene → liên kết room-to-room → đặt info hotspot → khai báo navigation/collision zone → preview → publish.</p></div></div>}
