import Link from'next/link';import{apiFetch,formatPrice}from'@/lib/api';import type{PropertyDetail}from'@/lib/types';export default async function Page(){const items=await apiFetch<PropertyDetail[]>('/admin/properties',{cache:'no-store'},true);return <div className="stack"><div className="row" style={{justifyContent:'space-between'}}><div><span className="eyebrow">Inventory</span><h1 style={{fontSize:'2.7rem'}}>Bất động sản</h1></div><Link className="btn btn-primary" href="/admin/properties/new">+ Đăng tin mới</Link></div><div className="table-wrap"><table><thead><tr><th>Tiêu đề</th><th>Giá</th><th>Khu vực</th><th>Trạng thái</th><th>3D</th><th>Cập nhật</th></tr></thead><tbody>{items.map((p)=><tr key={p.id}><td><Link href={`/admin/properties/${p.id}`}><strong>{p.title}</strong></Link><div className="muted">{p.slug}</div></td><td>{formatPrice(p.price,p.transaction_type)}</td><td>{p.district}</td><td><span className={`badge ${p.status==='published'?'badge-brand':''}`}>{p.status}</span></td><td>{p.has_3d?'Có':'—'}</td><td>{new Date(p.updated_at).toLocaleDateString('vi-VN')}</td></tr>)}</tbody></table></div></div>}
