import{notFound}from'next/navigation';import{apiFetch}from'@/lib/api';import{PropertyForm}from'@/components/admin/PropertyForm';import type{PropertyDetail}from'@/lib/types';export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;let property:PropertyDetail;try{property=await apiFetch(`/admin/properties/${id}`,{cache:'no-store'},true)}catch{notFound()}return <div className="stack"><div><span className="eyebrow">Edit listing</span><h1 style={{fontSize:'2.7rem'}}>Chỉnh sửa bất động sản</h1></div><PropertyForm property={property}/></div>}
