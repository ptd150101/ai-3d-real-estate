import{apiFetch}from'@/lib/api';import{StatusSelect}from'@/components/admin/StatusSelect';type A={id:string;full_name:string;phone:string;email?:string;scheduled_at:string;status:string;note?:string;property_id:string};export default async function Page(){const items=await apiFetch<A[]>('/admin/appointments',{cache:'no-store'},true);return <div className="stack"><div><span className="eyebrow">Scheduling</span><h1 style={{fontSize:'2.7rem'}}>Lịch xem nhà</h1></div><div className="table-wrap"><table><thead><tr><th>Khách hàng</th><th>Thời gian</th><th>Liên hệ</th><th>Ghi chú</th><th>Trạng thái</th></tr></thead><tbody>{items.map((x)=><tr key={x.id}><td><strong>{x.full_name}</strong><div className="muted">{x.property_id}</div></td><td>{new Date(x.scheduled_at).toLocaleString('vi-VN')}</td><td>{x.phone}<br/>{x.email}</td><td>{x.note||'—'}</td><td><StatusSelect kind="appointments" id={x.id} value={x.status} options={['pending','confirmed','completed','cancelled','no_show']}/></td></tr>)}</tbody></table></div></div>}
