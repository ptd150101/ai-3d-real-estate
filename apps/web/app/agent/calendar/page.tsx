"use client";
import { FormEvent, useEffect, useState } from "react";
import { clientApi } from "@/lib/client-api";
export default function Page(){const[rules,setRules]=useState<any[]>([]);const load=async()=>{setRules(await clientApi<any[]>("/agent/availability-rules"));};useEffect(()=>{void load();},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await clientApi("/agent/availability-rules",{method:"POST",body:JSON.stringify({weekday:Number(f.get("weekday")),start_minute:Number(f.get("start"))*60,end_minute:Number(f.get("end"))*60,slot_minutes:Number(f.get("slot")),buffer_minutes:15,timezone:"Asia/Ho_Chi_Minh",active:true})});await load();}return <section className="section"><div className="container stack"><h1>Lịch làm việc môi giới</h1><form className="card card-padded row-wrap" onSubmit={submit}><select name="weekday"><option value="0">Thứ Hai</option><option value="1">Thứ Ba</option><option value="2">Thứ Tư</option><option value="3">Thứ Năm</option><option value="4">Thứ Sáu</option><option value="5">Thứ Bảy</option><option value="6">Chủ Nhật</option></select><input name="start" type="number" min="0" max="23" defaultValue="9"/><input name="end" type="number" min="1" max="24" defaultValue="17"/><select name="slot" defaultValue="60"><option value="30">30 phút</option><option value="45">45 phút</option><option value="60">60 phút</option></select><button className="btn btn-primary">Thêm khung giờ</button></form>{rules.map(r=><article className="card card-padded row" key={r.id} style={{justifyContent:"space-between"}}><strong>Thứ {r.weekday+2}: {Math.floor(r.start_minute/60)}:00–{Math.floor(r.end_minute/60)}:00</strong><button className="btn btn-ghost" onClick={async()=>{await clientApi(`/agent/availability-rules/${r.id}`,{method:"DELETE"});await load();}}>Xóa</button></article>)}</div></section>}
