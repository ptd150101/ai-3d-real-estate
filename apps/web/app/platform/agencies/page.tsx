import { apiFetch } from "@/lib/api";
export default async function Page(){let cases:any[]=[];try{cases=await apiFetch<any[]>("/organizations/platform/verification",{cache:"no-store"},true);}catch{}return <section className="container section"><div className="section-heading"><div><span className="eyebrow">P2 · Platform</span><h1>Xác minh agency marketplace</h1></div></div><div className="data-grid">{cases.length?cases.map(item=><article className="panel" key={item.id}><strong>{item.organization_id}</strong><p>{item.status}</p><pre>{JSON.stringify(item.documents,null,2)}</pre></article>):<div className="empty-state">Không có hồ sơ đang chờ.</div>}</div></section>}
