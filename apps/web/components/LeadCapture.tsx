"use client";
import{FormEvent,useState}from'react';export function LeadCapture({propertyId}:{propertyId?:string}){const[done,setDone]=useState(false);const[error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const response=await fetch('/api/backend/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({property_id:propertyId||null,full_name:f.get('full_name'),phone:f.get('phone'),email:f.get('email')||null,message:'Yêu cầu tư vấn viên gọi lại từ chatbot',source:'chatbot'})});if(response.ok)setDone(true);else setError('Không thể gửi yêu cầu')}if(done)return <div className="alert success">Đã gửi yêu cầu. Tư vấn viên sẽ liên hệ lại.</div>;return <form className="tool-card stack" onSubmit={submit}><strong>Yêu cầu gọi lại</strong><input className="input" required name="full_name" placeholder="Họ tên"/><input className="input" required name="phone" placeholder="Số điện thoại"/><input className="input" type="email" name="email" placeholder="Email (không bắt buộc)"/>{error&&<div className="alert">{error}</div>}<button className="btn btn-primary btn-sm">Gửi cho tư vấn viên</button></form>}
