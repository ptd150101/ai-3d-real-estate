"use client";
import{FormEvent,useState}from'react';import{useRouter}from'next/navigation';export function KnowledgeForm(){const router=useRouter();const[error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const response=await fetch('/api/backend/knowledge',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({property_id:f.get('property_id')||null,project_id:null,document_type:f.get('document_type'),title:f.get('title'),source_url:f.get('source_url')||null,content:f.get('content'),verified:f.get('verified')==='on'})});if(response.ok){e.currentTarget.reset();router.refresh()}else setError('Không thể lập chỉ mục tài liệu')}return <form className="card card-padded stack" onSubmit={submit}><h3>Thêm tài liệu RAG</h3><div className="form-grid"><label className="label">Loại<select className="select" name="document_type"><option value="faq">FAQ</option><option value="legal">Pháp lý</option><option value="policy">Chính sách</option><option value="listing">Tin đăng</option></select></label><label className="label">Property ID<input className="input" name="property_id"/></label><label className="label full">Tiêu đề<input required className="input" name="title"/></label><label className="label full">Nguồn URL<input className="input" name="source_url"/></label><label className="label full">Nội dung<textarea required className="textarea" name="content" style={{minHeight:200}}/></label><label className="row full"><input type="checkbox" name="verified"/> Được phép dùng cho câu trả lời đã xác minh</label></div>{error&&<div className="alert">{error}</div>}<button className="btn btn-primary">Lưu và lập chỉ mục</button></form>}
